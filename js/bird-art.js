// ============================================================
// ★★飛ぶ敵（空にいる敵）—— ★★これは「仮の絵」です ★★
// ============================================================
//
//   ■ ★★AIが置いた仮の絵です。島さんの絵ではありません
//
//     島さん（2026-08-23）:
//     > 「飛ぶ敵。空中にいる敵。いまはタップし放題（跳んでも損がない）ので迷いがありません。
//     >   空に敵がいると『ここは跳ばない』が初めて生まれます。
//     >   1ボタンのまま、判断だけが増える。」
//     > 「エンディング後に出現にしてください。」
//
//     ★形と色は AI が仮に置いたもの。**島さんが描いたら、そのまま差し替えます。**
//
//   ■ ★差し替えるときの手順（★丘の敵とまったく同じ・3手）
//     ① `assets/parts/` に絵を保存する（★`.aseprite` でも `.png` でもよい）
//     ② 「絵を反映する.bat」をダブルクリック（→ `js/parts-art.js` ができる）
//     ③ `js/ollie.js` の `BIRD` を、その呼び名から読むように1行変える
//        （★丘の敵 `ENEMY` が `js/enemy-art.js` を経由しているのと同じ形にする）
//
//   ■ ★★★守ってほしい決まり（★描き替えるときも）
//
//     **① 必ず黒（`9`）で縁取る。**
//        ★理由: 空には雲も山もあるので、縁取りが無いと
//          「ぶつかるもの」と「ただの背景」が見分けられない。
//        ★`test/sprites.test.js`【4.5】が輪郭を1ドットずつ見張っています。
//
//     **② ★★「空にいる」と、ひと目で分かる形にする。**
//        ★★★この敵は**地上では絶対に当たりません**（→ `js/ollie.js` の `hitConeIndex()`）。
//          ★「くぐれば安全」と伝わらないと、★★players は無意味に避けようとします。
//        ★だから**翼を広げた形**にしてあります（★地面の物には見えない）。
//
//     **③ ★横に広すぎないこと。**
//        ★当たり判定は**絵の幅そのもの**です（★`obWidth()`）。
//          ★広いと「跳んではいけない区間」が長くなりすぎて、★★息が詰まります。
//        ★いまは **11ドット**（★丘の敵は13、コーンは9）。
//
//   ■ 中身（`js/cone-art.js` と同じ形）
//     FRAMES   … 1コマ = { x, y, rows }。rows の1文字 = `js/palette.js` の色の番号
//     FEET_ROW … 絵の中で「足がつく」いちばん下の行。
//                ★★★飛ぶ敵は地面に立たないので、これは**絵の下端**という意味だけ。
//                  ★どれだけ浮かせるかは `js/ollie.js` の `BIRD_LIFT` が決めます
//
//   ★色そのものは `js/palette.js` にある（ここには持たない）
//     9 = まっ黒（縁取り） / e = 暗い灰（体） / f = 明るい灰（翼の内側）
// ============================================================
(function (global) {
	"use strict";

	global.DotBirdArt = {
		SOURCE: "（AIが置いた仮の絵。島さんが描いたら差し替える）",
		FEET_ROW: 6,
		FRAMES: [{
			x: 0, y: 0, rows: [
				"..9.....9..",
				".9e9...9e9.",
				"9eff9.9ffe9",
				".9eff9ffe9.",
				"..9efffe9..",
				"...9eee9...",
				"....999...."
			]
		}]
	};
})(typeof window !== "undefined" ? window : globalThis);
